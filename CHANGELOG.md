# 更新日志

> 本文档记录续聊助手的版本变化；使用教程请见 [README.md](README.md)。

### 1.4.3

官方聊天框重试助手降噪和安全性增强。
- 降低会话同步 ping 频率，缓存端口归属，并对失败探测做退避。
- 检测 CTH AutoChat 等外部 workbench hook，面板提示可能的自动重试冲突。
- 自动重试只处理明确的限流、账单、402 类弹窗，并绑定触发弹窗的消息气泡，减少误点。
- 面板新增自动重试暂停/启用控制，通过本机 `/lca/control` 同步到已注入 helper。

### 1.4.2

多会话性能与官方上下文转接增强。

- MCP server 内新增全局队列共享 watcher/cache，多个 session 复用一次 `global_queue.json` stat/read 唤醒；实际领取仍在锁内完成，避免抢消息。
- 官方聊天框 helper 使用 pane 级 `WeakMap` 缓存 `{ agentId, title }`，并通过 `/lca/active` 把标题同步给插件侧。
- 启动指令使用 `会话 N` 作为官方聊天标题种子，减少 “MCP bridge conversation” 这类自动命名。
- 转接流程强化 Cursor 官方 conversation/composer ID 展示和注入，便于目标会话按官方 ID 定位原上下文。
- 轮询调度只读取候选会话队列长度，减少持锁期间的状态和信标读取。

### 1.4.1

面板状态刷新改为纯本地状态驱动，不再在刷新路径依赖 Cursor SQLite。会话详情仍可按需读取官方历史。顶部栏响应式也做了整理。

### 1.3.x

模块化、健壮性和安全增强：

- `extension.js` 拆分为多个 `src/` 模块。
- 新增独立窗口/多窗口面板。
- shell 命令转义加固。
- 目录锁、JSONL 裁剪、JSON 缓存、项目目录校验等基础设施优化。
- 命令面板操作支持选择会话。

### 1.2.x

MCP 能力补齐：

- 增加 presence 心跳，长任务不再轻易误判中断。
- `wait_for_instruction` 增加结构化输出。
- 新增 `get_status` 和 MCP 资源。
- 新增引用文件夹和项目目录限定。

### 1.1.x

重新引入本机回环通道，用于官方聊天框活动会话同步。安装重试补丁后，官方聊天框图标旁可显示 `#N` 会话序号，切换官方对话时面板尽量自动选中对应会话。

### 1.0.x

以 MCP 为默认传输，shell 作为兜底；移除旧 HTTP Bridge 和 Python 运行时；精简设置项，保留合理默认值。

### 0.8.x

引入 MCP 传输层、presence beacon、自动会话标识、自动会话标题、会话转接和实时执行状态。

### 0.7.x

重试补丁和 shell 心跳迭代，修复付费墙/限流自动重试、OOM 风险和错误气泡隐藏策略。

### 0.6.x 及更早

初始多会话、多队列、保活、调度、附件和重试补丁能力。
